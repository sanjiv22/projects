<%= form_for :article , url: articles_path do |f| %>
	<% if @article.error.any? %>
			<%=pluralize(@article.error.count,"error")%> prohibited
			this article from being saved:
		<% @article.error.full_messages.each do |msg| %>
			<%= msg %>
		 	<% end %>
	<% end %>

	<p>
		<%= f.label :title %><br>
		 <%= f.text_field :title %>		
	</p>

	<p>
		<%= f.label :text %><br>
		 <%= f.text_area :text %>		
	</p>

	<p>
		<%= f.submit %>
	</p>

<% end %>

<%= link_to 'back',articles_path %>
